새로운 feature를 논의하고, spec 문서를 작성한 뒤 feature-list.json에 등록합니다.

이 명령어는 다음 단계를 수행합니다:
1. 사용자와 feature에 대해 논의하여 요구사항 파악
2. docs 폴더에 spec 문서 작성 (`ai-workflow:create-spec-doc` 스킬 사용)
3. feature-list.json에 새 feature 등록 (`ai-workflow:create-feature-list` 스킬 참조)

---

## 0. 필수 파일 확인

```bash
ls -la feature-list.json 2>&1
mkdir -p docs
```

`feature-list.json` 파일이 없으면:

---

**설정이 필요합니다**

`feature-list.json` 파일이 없습니다.
먼저 `/ai-workflow:setup` 명령어를 실행하여 프로젝트를 설정해주세요.

---

**파일이 없으면 여기서 중단하세요.**

---

## 1. Feature 논의 (Discovery)

사용자와 대화를 통해 feature를 명확히 합니다.

**질문 가이드:**

> 추가하려는 feature에 대해 알려주세요:
>
> 1. **기능 이름**: 이 기능을 뭐라고 부르면 좋을까요?
> 2. **목적**: 이 기능이 왜 필요한가요? 어떤 문제를 해결하나요?
> 3. **사용자 시나리오**: 사용자가 이 기능을 어떻게 사용하게 되나요?
> 4. **우선순위**: 얼마나 급한가요? (high/medium/low)
> 5. **관련 정보**: 참고할 디자인, 문서, 또는 예시가 있나요?

**추가 질문 (필요시):**
- 기존 기능과의 관계가 있나요?
- 기술적 제약사항이 있나요?
- 예상되는 엣지 케이스가 있나요?

**명확해질 때까지 질문을 계속하세요.** 추측하지 마세요.

---

## 2. Feature 요약 확인

논의 내용을 정리하여 사용자에게 확인받습니다:

---

**Feature 요약**

| 항목 | 내용 |
|------|------|
| 기능명 | {feature_name} |
| 목적 | {purpose} |
| 우선순위 | {priority} |

**주요 요구사항:**
- {requirement_1}
- {requirement_2}
- {requirement_3}

**사용자 시나리오:**
1. {scenario_step_1}
2. {scenario_step_2}
3. {scenario_step_3}

이 내용이 맞나요? 수정할 부분이 있으면 알려주세요.

---

**사용자가 확인할 때까지 다음 단계로 진행하지 마세요.**

---

## 3. Spec 문서 작성

`ai-workflow:create-spec-doc` 스킬을 사용하여 spec 문서를 생성합니다.

**핵심 단계:**
1. `docs/` 폴더 확인/생성
2. 파일명 결정: `docs/spec-{feature-name}.md`
3. `templates/spec.md.template` 템플릿 사용
4. 논의 내용으로 플레이스홀더 치환
5. 파일 저장

**작성 규칙:**
- 논의한 내용을 빠짐없이 포함
- 불확실한 부분은 `TBD` 또는 `TODO`로 표시
- 체크박스로 진행 상황 추적 가능하게

---

## 4. Spec 문서 리뷰

작성한 spec 문서를 사용자에게 보여주고 피드백을 받습니다:

> Spec 문서를 작성했습니다: `docs/spec-{feature-name}.md`
>
> 내용을 확인하고 수정이 필요한 부분이 있으면 알려주세요.
> - 추가할 내용이 있나요?
> - 수정할 내용이 있나요?
> - 삭제할 내용이 있나요?

**사용자가 승인할 때까지 수정을 반복하세요.**

---

## 5. feature-list.json 업데이트

`ai-workflow:create-feature-list` 스킬의 `feature-item.json.template`을 참조하여 새 feature를 추가합니다.

```bash
cat feature-list.json
```

**핵심 단계:**
1. 기존 feature-list.json 읽기
2. 새 ID 생성 (기존 최대 ID + 1)
3. 새 feature 항목 추가
4. `lastUpdated` 필드 업데이트
5. 파일 저장

**새 feature 항목 필드:**
- `id`: `feature-{NNN}`
- `title`: 기능명
- `description`: 간단한 설명
- `status`: `pending`
- `priority`: 사용자가 지정한 우선순위
- `specPath`: `docs/spec-{feature-name}.md`
- `relatedFiles`: `[]`
- `notes`: `""`

---

## 6. 완료 보고

---

**Feature 추가 완료**

**등록된 Feature:**
- ID: `{feature_id}`
- 제목: {title}
- 우선순위: {priority}
- 상태: pending

**생성된 문서:**
- 📄 `{spec_path}`

**다음 단계:**
- `/ai-workflow:initialize`로 작업 컨텍스트 로드
- `/ai-workflow:recommend-agent`로 적합한 agent 추천받기

---

## 주의사항

- Feature에 대해 충분히 이해할 때까지 질문하세요
- Spec 문서는 사용자 승인 후에만 저장하세요
- feature-list.json은 기존 내용을 유지하면서 추가하세요
- 모든 단계에서 사용자 확인을 받으세요
- 각 스킬의 상세 내용은 해당 스킬 문서 참조
